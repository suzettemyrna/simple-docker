services:
  hello_server:
    build: ../Part5
    container_name: hello_server
    tty: true
  hello_nginx:
    image: nginx
    container_name: hello_nginx
    ports:
      - 80:8080
    depends_on:
      - hello_server
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf